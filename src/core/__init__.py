"""Core engine: predictive detection + the tiles play loop.

`bot` (BotEngine / BotConfig, the `_run_tiles` loop + auto-lead tuner) and
`predict` (pure predictive-detection primitives: tile mask, lane segments,
velocity, press scheduling).
"""
