"""Per-game bot integrations built on the shared vision/predict core.

Each module here pairs the game-agnostic predictive tracker (`src/core/predict.py`)
with one game's geometry, note appearance, and input channel. `wwm` is the
Where Winds Meet (6-lane keyboard rhythm) integration.
"""
