"""Input backends.

`touch` (TouchInjector — background Win32 InjectTouchInput multi-touch, the
primary backend) and `ld_click` (LDMessageClicker — message-click into
LDPlayer's render child, an experimental fallback for ad surfaces touch misses).
"""
