"""Capture + window targeting.

`window_capture` (WindowCapture — dxcam / PrintWindow HWND grab), `screen`
(ScreenCapture — mss, for the eyedropper / scale), and `window_picker`
(list_windows / focus_window).
"""
